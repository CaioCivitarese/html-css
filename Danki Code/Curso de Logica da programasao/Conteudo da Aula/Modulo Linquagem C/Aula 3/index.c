#include <stdio.h>
#include <stdlib.h>

int main(){

    int hora_cinema = 20;
    int hora_atual= 20;

    if (hora_atual > hora_cinema + 30)
    {
        printf("Passou do horario! Não pode entrar!");
    }else if (hora_atual < hor_cinema)
    {
        printf("Nem abriu a porta do cinema");
    }else{
        printf("O horario esta cento! Pode entrar!")
    }
    
    
    result 0;
}