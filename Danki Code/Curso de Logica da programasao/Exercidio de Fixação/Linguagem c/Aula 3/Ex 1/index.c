#include <stdio.h>
#include <stdlib.h>

int main(){

    int hora_fecha = 20;
    int hora_atual = 18;

    if (hora_atual > hora_fecha)
    {
        printf("A picina esta fechada, volte amanha");
    }else if (hora_atual < hora_fecha)
    {
        prinf("A pricina esta aberta, boa diverção");
    }else
    {
        printf("Ola doutor estranho, volta ao tempo certo");
    }

    result 0;
}